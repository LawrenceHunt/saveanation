{
  "name": "saveanation",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "test": "CUCUMBER_BROWSERS=2 chimp --ddp=http://localhost:3000 --mocha || exit 0",
    "test-watch": "CUCUMBER_BROWSERS=2 chimp --ddp=http://localhost:3000 --mocha --watch || exit 0",
    "practical": "meteor test --driver-package=practicalmeteor:mocha"
  },
  "dependencies": {
    "meteor-node-stubs": "~0.2.0",
    "babel-runtime": "6.18.0",
    "bcrypt": "^0.8.7"
  }
}
